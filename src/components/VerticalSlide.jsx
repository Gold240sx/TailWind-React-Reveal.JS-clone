import { Children } from "react"

const VerticalSlide = ({ id, children }) => {
	// const { id } = props
	// console.log(id)
	return <div className="w-full h-full VerticalSlide">{children}</div>
}

export default VerticalSlide
